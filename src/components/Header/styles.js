import { makeStyles } from "@material-ui/styles";

const useStyles = makeStyles(theme => ({
  title: {
    flexGrow: 1
  },
  toolbar: {
    paddingLeft: 0,
    paddingRight: 0
  }
}));

export { useStyles };
